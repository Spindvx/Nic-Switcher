# UTF-8
#
# PyInstaller reads this file via the version= argument on EXE() and
# embeds it as Win32 VERSIONINFO in the resulting exe. Inno Setup's
# GetFileVersion() reads it back, so the installer picks up the app
# version automatically without you having to keep the .iss in sync.
#
# Format reference: https://learn.microsoft.com/en-us/windows/win32/menurc/versioninfo-resource
#
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(0, 2, 0, 0),
    prodvers=(0, 2, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo([
      StringTable(
        u'040904B0',  # langid=EN-US, codepage=1200 (UTF-16)
        [
          StringStruct(u'CompanyName',      u'Spindvx'),
          StringStruct(u'FileDescription',  u'NIC Switcher — NIC IP presets + DHCP for Pro AV'),
          StringStruct(u'FileVersion',      u'0.2.0.0'),
          StringStruct(u'InternalName',     u'NICSwitcher'),
          StringStruct(u'OriginalFilename', u'NICSwitcher.exe'),
          StringStruct(u'ProductName',      u'NIC Switcher'),
          StringStruct(u'ProductVersion',   u'0.2.0'),
        ])
    ]),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])])
  ]
)
